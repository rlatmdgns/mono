interface SurveyOptionsProps {
  index: number
  type: any
}

export const FreshmanSurveyOptions = ({ index, type }: SurveyOptionsProps) => {
  return (
    <>
      {/*<SurveyRadioOption index={index} type={type} />*/}
      {/*<SurveyCheckboxOption index={index} type={type} />*/}
      {/*<SurveyEssayOption index={index} type={type} />*/}
    </>
  )
}
