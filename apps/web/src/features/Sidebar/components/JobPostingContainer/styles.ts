import styled from 'styled-components'

export const Wrapper = styled.div`
  max-height: calc(100vh - 480px);
  margin: 0 -12px auto -8px;
  overflow: auto;
`

export const Empty = styled.span`
  margin-top: 8px;
`
