import styled from 'styled-components'

export const MemberUtils = styled.div`
  display: flex;
  gap: 8px;
  position: absolute;
  top: 0;
  right: 20px;

  > button {
    width: 120px;
  }
`

export const AddMember = styled.div`
  min-width: 118px;
  flex-shrink: 0;
`
