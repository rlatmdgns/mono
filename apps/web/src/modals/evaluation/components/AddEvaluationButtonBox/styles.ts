import styled from 'styled-components'

export const Wrapper = styled.div`
  top: calc(100% + 8px);
  display: flex;
  border-radius: 8px;
  box-shadow: 0 4px 20px 0 rgba(161, 176, 203, 0.2);
  padding: 4px;
  margin-top: 8px;
`
