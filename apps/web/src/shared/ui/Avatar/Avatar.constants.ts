import { SizeOptions } from './Avatar.interface'

export const SIZE_OPTIONS: SizeOptions = {
  xs: 16,
  sm: 20,
  md: 32,
  lg: 40,
}

export const FONT_SIZE_OPTIONS = {
  xs: 9,
  sm: 11,
  md: 14,
  lg: 16,
}
